# Admin JS modules (v4.10.89)

Load: core → preview → cta → persist → tree → save → tabs → items-ui → form → diagnostics → admin.js

admin.js lines: 1559 (target <1200)
Breakpoint: .cgs-is-mobile only (no fixed 768 media)
